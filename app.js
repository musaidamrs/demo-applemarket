'use strict';
const $=(s,root=document)=>root.querySelector(s), $$=(s,root=document)=>[...root.querySelectorAll(s)];
const icons=solarIcons;
const icon=n=>`<svg viewBox="0 0 24 24" aria-hidden="true">${icons[n]||icons.arrow}</svg>`;
const escapeHTML=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const money=n=>new Intl.NumberFormat('ru-RU').format(n)+' ₽';
const cats=['Все','iPhone','Mac','iPad','Watch','AirPods'];
function readStorage(k,d){try{return JSON.parse(localStorage.getItem(k))??d}catch{return d}}
let cart=readStorage('yabloko-cart',[]),favorites=readStorage('yabloko-favorites',[]);cart=Array.isArray(cart)?cart.filter(x=>products.some(p=>p.id===x.id)&&Number.isInteger(x.qty)&&x.qty>0&&x.qty<=99&&Number.isFinite(x.price)&&x.price>0):[];favorites=Array.isArray(favorites)?favorites.filter(x=>products.some(p=>p.id===x)):[];
function save(){try{localStorage.setItem('yabloko-cart',JSON.stringify(cart));localStorage.setItem('yabloko-favorites',JSON.stringify(favorites))}catch{}updateCount()}
const page=document.body.dataset.page;const params=new URLSearchParams(location.search);
function brand(){return '<a class="brand" href="index.html" aria-label="APPLE MARKET — главная"><img class="brand-mark" src="assets/apple-market-mark.svg" alt="" width="32" height="34"><span>APPLE MARKET</span></a>'}
function shell(){const nav=[['home','Главная','index.html','home'],['catalog','Каталог','catalog.html','catalog'],['cart','Корзина','cart.html','bag'],['favorites','Избранное','favorites.html','heart'],['chat','Чат','#','chat']];const active=page==='product'?'catalog':page;document.body.innerHTML=`<canvas id="matrix" aria-hidden="true"></canvas><div class="container"><header class="header"><button class="icon-btn top-control" data-open="search-dialog" aria-label="Поиск">${icon('search')}<span class="search-label">Поиск</span></button>${brand()}<div class="header-actions" role="group" aria-label="Тема и меню"><button class="icon-btn theme-toggle" id="theme-toggle" aria-label="Включить тёмную тему" aria-pressed="false"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M20.2 15.1A8.6 8.6 0 0 1 8.9 3.8a8.6 8.6 0 1 0 11.3 11.3Z" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linejoin="round"/></svg></button><button class="icon-btn menu-trigger" data-open="menu-dialog" aria-label="Открыть меню" aria-haspopup="dialog">${icon('menu')}</button></div></header><main id="main"></main></div><footer class="footer"><div class="container"><div class="footer-inner"><div>${brand()}<p>Технологии, которые становятся вашими.</p></div><div><h4>Устройства</h4>${cats.slice(1).map(c=>`<a href="catalog.html?category=${c}">${c}</a>`).join('')}</div><div><h4>Поддержка</h4><span class="footer-label">О магазине</span><span class="footer-label">Доставка и оплата</span><span class="footer-label">Помощь и контакты</span></div></div><div class="footer-bottom"><span>© ${new Date().getFullYear()} APPLE MARKET</span><span>Демонстрационная витрина · Цены примерные</span><span>Иконки Solar · CC BY 4.0</span></div></div></footer><nav class="dock" aria-label="Основная навигация">${nav.map(([id,label,url,glyph])=>id==='chat'?`<button class="dock-item" data-open="chat-dialog" aria-label="Чат обратной связи">${icon(glyph)}<span>${label}</span></button>`:`<a class="dock-item ${active===id?'active':''}" href="${url}" aria-label="${label}" ${active===id?'aria-current="page"':''}>${icon(glyph)}<span>${label}</span>${id==='cart'?'<span class="count" id="cart-count" hidden></span>':''}</a>`).join('')}</nav><dialog class="chat" id="chat-dialog" aria-label="Чат обратной связи"><div class="chat-head"><div><strong>APPLE MARKET</strong><small>Помощник по выбору · без оператора</small></div><button class="icon-btn" data-close aria-label="Закрыть чат">${icon('close')}</button></div><div class="messages" id="messages" aria-live="polite"><div class="message">Здравствуйте! Помогу разобраться в устройствах Apple. Что выбираете?</div></div><div class="chat-quick"><button data-question="Помоги выбрать iPhone">iPhone Duo</button><button data-question="Нужен ноутбук для работы">Для работы</button><button data-question="Как оформить заказ?">Как заказать?</button></div><form class="chat-form"><input aria-label="Сообщение помощнику" placeholder="Ваш вопрос…" required maxlength="500"><button aria-label="Отправить">${icon('arrow')}</button></form></dialog><dialog class="search-dialog" id="search-dialog" aria-label="Поиск по каталогу"><div class="dialog-top"><h2>Найдём ваше.</h2><button class="icon-btn" data-close aria-label="Закрыть поиск">${icon('close')}</button></div><input class="search-input" type="search" placeholder="iPhone, MacBook, AirPods…" aria-label="Поиск товаров"><div class="search-results"></div></dialog><dialog class="menu-dialog" id="menu-dialog" aria-label="Меню"><div class="dialog-top"><span class="menu-wordmark">APPLE MARKET</span><button class="icon-btn" data-close aria-label="Закрыть меню">${icon('close')}</button></div><nav><a href="catalog.html">Каталог ${icon('arrow')}</a><a href="about.html">О магазине ${icon('arrow')}</a><a href="delivery.html">Доставка и оплата ${icon('arrow')}</a><a href="support.html">Помощь ${icon('arrow')}</a></nav></dialog><div class="toast" role="status" aria-live="polite"></div>`;updateCount()}
function updateCount(){const el=$('#cart-count');if(el){const n=cart.reduce((s,x)=>s+x.qty,0);el.textContent=n;el.hidden=!n}}
let toastTimer;function toast(t){$('.toast').textContent=t;$('.toast').classList.add('show');clearTimeout(toastTimer);toastTimer=setTimeout(()=>$('.toast').classList.remove('show'),3000)}
function breadcrumbs(title){return `<div class="breadcrumbs"><a href="index.html">Главная</a><span>/</span><span>${escapeHTML(title)}</span></div>`}
function productURL(p){return 'product.html?id='+p.id}
function cardSlides(p,state){return galleryFor(p,state.color).map((src,i)=>`<div class="card-slide" aria-hidden="${i!==state.index}"><img src="assets/${src}" alt="${p.name} — ${p.colors[state.color]}, фото ${i+1}" loading="lazy" width="600" height="600" draggable="false"></div>`).join('')}
function cardDots(p,state){return galleryFor(p,state.color).map((_,i)=>`<button class="photo-dot ${i===state.index?'active':''}" data-card-index="${i}" data-product-id="${p.id}" aria-label="${p.name}: фото ${i+1}" aria-pressed="${i===state.index}"><span></span></button>`).join('')}
function card(p){const state=cardSelections[p.id]||{color:0,index:0};return `<article class="card" data-card-id="${p.id}"><button class="icon-btn favorite ${favorites.includes(p.id)?'selected':''}" data-favorite="${p.id}" aria-label="${favorites.includes(p.id)?'Убрать из избранного':'В избранное'}: ${p.name}" aria-pressed="${favorites.includes(p.id)}">${icon(favorites.includes(p.id)?'heartFill':'heart')}</button><div class="card-image"><div class="card-viewport" data-swipe-card="${p.id}" tabindex="0" role="group" aria-roledescription="карусель" aria-label="Фотографии ${p.name}. Листайте влево и вправо"><div class="card-track" style="transform:translateX(-${state.index*100}%)">${cardSlides(p,state)}</div></div><div class="photo-dots" aria-label="Ракурсы ${p.name}">${cardDots(p,state)}</div></div><a class="card-link" href="${productURL(p)}"><h3>${p.name}</h3></a><div class="card-colors" aria-label="Цвет ${p.name}">${p.colors.map((c,i)=>`<button class="color-dot ${i===state.color?'active':''}" style="--swatch:${colorHex(c)}" data-card-color="${i}" data-product-id="${p.id}" aria-label="${p.name}: ${c}" title="${c}" aria-pressed="${i===state.color}"></button>`).join('')}</div>${p.availability?'<p class="availability">С 23 октября</p>':''}<div class="card-bottom"><span class="price">от ${money(p.price)}</span><button class="add-btn" data-add="${p.id}" aria-label="Добавить ${p.name} в корзину">${icon('plus')}</button></div></article>`}
function home(){return `<section class="hero" aria-label="Баннер iPhone Duo"><div class="hero-copy"><h1>iPhone Duo.<br>Больше мира.</h1><p>Ваш мир. Теперь без границ.</p><a class="pill iridescent" href="product.html?id=iphone-duo">Выбрать Duo ${icon('arrow')}</a></div><div class="hero-visual"><img id="hero-gif" src="assets/iphone-duo-hero-still.jpg" alt="iPhone Duo раскрывается и поворачивается — анимация Apple" width="800" height="530"></div><button class="video-toggle" id="video-toggle" aria-label="Приостановить видео" aria-pressed="false"><span aria-hidden="true">Ⅱ</span></button></section><div class="second-row"><section class="portrait-banner" aria-label="Баннер MacBook Air 3:4"><h2>Лёгкий.<br>С силой M5.</h2><img src="assets/mac-2026.png" alt="MacBook Air с чипом M5" width="980" height="469"><a class="pill iridescent" href="product.html?id=macbook-air">MacBook Air ${icon('up')}</a></section><section class="editorial"><h2>Больше, чем<br>технологии.</h2><p>Для идей, которые не отпускают.<br>Людей, которые рядом.<br>Моментов, которые остаются.</p><a class="pill outline" href="catalog.html">Найти своё ${icon('arrow')}</a></section></div><section class="section"><div class="section-title"><h2>Ваша следующая история.</h2><a class="text-link" href="catalog.html">Все устройства ${icon('arrow')}</a></div><div class="cards">${products.map(card).join('')}</div></section>`}
function catalog(){return `${breadcrumbs('Каталог')}<h1 class="page-heading">Найдите своё.</h1><p class="lead">Устройства Apple для работы, творчества и всего, что между.</p><div class="catalog-layout"><div class="catalog-content"><div class="catalog-search">${icon('search')}<input id="filter-search" type="search" aria-label="Поиск в каталоге" placeholder="Найти устройство"></div><div class="catalog-tabs">${cats.map(c=>`<button class="chip ${c===(params.get('category')||'Все')?'active':''}" data-category="${c}">${c}</button>`).join('')}</div><div class="results-top"><span id="results-count" aria-live="polite"></span><div class="sort-wrap"><input type="hidden" id="sort" value="default"><button class="sort-trigger" id="sort-trigger" aria-haspopup="menu" aria-expanded="false" aria-controls="sort-menu"><span id="sort-label">По умолчанию</span>${icon('arrow')}</button><div class="sort-menu" id="sort-menu" role="menu" aria-label="Сортировка" hidden>${[['default','По умолчанию'],['asc','Сначала дешевле'],['desc','Сначала дороже'],['name','По названию']].map(([v,t])=>`<button role="menuitemradio" aria-checked="${v==='default'}" data-sort="${v}"><span>${t}</span><span class="sort-check" aria-hidden="true">✓</span></button>`).join('')}</div></div></div><div class="cards" id="catalog-cards"></div></div></div><p class="demo-note">Демонстрационный ассортимент. Цены и комплектации требуют подтверждения магазином.</p>`}
let selectedCategory=cats.includes(params.get('category'))?params.get('category'):'Все';
function renderCatalog(){let list=products.filter(p=>(selectedCategory==='Все'||p.category===selectedCategory)&&p.name.toLowerCase().includes($('#filter-search').value.toLowerCase()));const sort=$('#sort').value;if(sort==='asc')list.sort((a,b)=>a.price-b.price);if(sort==='desc')list.sort((a,b)=>b.price-a.price);if(sort==='name')list.sort((a,b)=>a.name.localeCompare(b.name));$('#results-count').textContent=`Устройств: ${list.length}`;$('#catalog-cards').innerHTML=list.length?list.map(card).join(''):'<div class="empty"><h2>Пока ничего не нашлось</h2><p>Измените запрос или выберите другую категорию.</p><button class="pill outline" data-reset>Сбросить фильтры</button></div>';$$('[data-category]').forEach(b=>{b.classList.toggle('active',b.dataset.category===selectedCategory);b.setAttribute('aria-pressed',String(b.dataset.category===selectedCategory))})}
const cardSelections={};let galleryIndex=0;
let selectedProduct=products.find(p=>p.id===params.get('id')),selectedMemory=0,selectedColor=0;
function productPage(){const p=selectedProduct;if(!p)return `${breadcrumbs('Товар не найден')}<div class="empty"><h1>Такого устройства нет в каталоге</h1><a class="pill" href="catalog.html">Перейти в каталог</a></div>`;document.title=`${p.name} — APPLE MARKET`;return `${breadcrumbs(p.name)}<div class="product-layout">${productGallery(p)}<section class="product-copy"><h1>${p.name}</h1><p>${p.description}</p>${p.availability?`<p class="availability">${p.availability}</p>`:''}${p.memory.length?`<span class="option-label">Память</span><div class="options">${p.memory.map((v,i)=>`<button class="option ${i===0?'active':''}" data-memory="${i}" aria-pressed="${i===0}">${v}</button>`).join('')}</div>`:''}<span class="option-label">Цвет: <span id="color-name">${p.colors[0]}</span></span><div class="options">${p.colors.map((v,i)=>`<button class="option ${i===0?'active':''}" data-color="${i}" aria-pressed="${i===0}">${v}</button>`).join('')}</div>${p.colorImages?'':'<p class="demo-note">Фото иллюстрирует модель; выбранный цвет может отличаться.</p>'}<span class="price" id="product-price">${money(p.price)}</span><button class="pill iridescent" id="product-add">Добавить в корзину ${icon('plus')}</button><p class="demo-note">Примерная цена. Доступность и точная комплектация пока не подтверждены.</p></section></div><section class="specs"><h2>В деталях.</h2><a class="source-link" href="${p.source}" target="_blank" rel="noopener">Характеристики на apple.com ${icon('up')}</a>${Object.entries(p.specs).map(([k,v])=>`<div class="spec-row"><span>${k}</span><strong>${v}</strong></div>`).join('')}<details><summary>Как выбрать память?</summary><p>Для обычных приложений и облачного хранения можно начать с базового объёма. Если храните много фото, видео или рабочих файлов на устройстве, рассмотрите больший объём.</p></details><details><summary>Как оформить покупку?</summary><p>Сейчас доступна демонстрационная корзина. Вы можете сохранить список выбранных устройств; оплата и передача заказа магазину ещё не подключены.</p></details></section>`}
function addProduct(p,m=0,c=0){const memory=p.memory[m]||'',color=p.colors[c],key=[p.id,memory,color].join('|'),price=p.price+m*10000;const row=cart.find(x=>x.key===key);if(row){if(row.qty>=99){toast('Максимум 99 устройств в одной позиции');return}row.qty++}else cart.push({key,id:p.id,memory,color,price,qty:1});save();toast(`${p.name} добавлен в корзину`)}
function cartPage(){return `${breadcrumbs('Корзина')}<h1 class="page-heading">Ваш выбор.</h1><div id="cart-body"></div>`}
function renderCart(){if(!cart.length){$('#cart-body').innerHTML='<div class="empty"><h2>Ваша корзина пока пуста</h2><p>Самое интересное ещё впереди. Найдите устройство, которое станет вашим.</p><a class="pill" href="catalog.html">Открыть каталог</a></div>';return}const total=cart.reduce((s,x)=>s+x.price*x.qty,0);$('#cart-body').innerHTML=`<div class="cart-layout"><div>${cart.map((x,i)=>{const p=products.find(p=>p.id===x.id);return `<article class="cart-row"><a href="${productURL(p)}"><img src="assets/${p.image}" alt="${p.name}"></a><div><a href="${productURL(p)}"><h3>${p.name}</h3></a><p>${escapeHTML(x.memory)} ${escapeHTML(x.color)}</p><div class="quantity"><button data-qty="${i}" data-delta="-1" aria-label="Уменьшить количество ${p.name}">−</button><span>${x.qty}</span><button data-qty="${i}" data-delta="1" aria-label="Увеличить количество ${p.name}">+</button></div></div><div><span class="price">${money(x.price*x.qty)}</span><button class="icon-btn" data-remove="${i}" aria-label="Удалить ${p.name}">${icon('close')}</button></div></article>`}).join('')}</div><aside class="summary"><h2>Всё вместе</h2><div class="spec-row"><span>Устройств</span><strong>${cart.reduce((s,x)=>s+x.qty,0)}</strong></div><div class="spec-row"><span>Примерная сумма</span><strong>${money(total)}</strong></div><button class="pill" id="download-order">Сохранить подборку ${icon('arrow')}</button><p>Скачайте список выбранных устройств. Это не заказ: онлайн-оплата и отправка менеджеру пока не подключены.</p></aside></div>`}
function favoritePage(){const list=products.filter(p=>favorites.includes(p.id));return `${breadcrumbs('Избранное')}<h1 class="page-heading">То, что нравится.</h1><p class="lead">Сохранённые устройства — всегда под рукой в этом браузере.</p><div class="cards">${list.length?list.map(card).join(''):'<div class="empty"><h2>Здесь будет ваш личный выбор</h2><p>Нажмите на сердечко рядом с устройством, чтобы сохранить его.</p><a class="pill" href="catalog.html">Посмотреть устройства</a></div>'}</div>`}
function infoPage(type){if(type==='about')return `${breadcrumbs('О магазине')}<div class="info-page"><h1 class="page-heading">Техника — часть жизни.<br>Выбор — её приятная часть.</h1><p class="lead">Собрали Apple в одном месте, чтобы найти своё было проще.</p><div class="info-card"><h2>Начните с того, что важно вам</h2><p>Работа на ходу, творчество, фотографии или любимая музыка. Мы расположили устройства по категориям и оставили главные параметры рядом — без лишнего шума.</p><a class="text-link" href="catalog.html">Знакомиться с коллекцией ${icon('arrow')}</a></div><div class="info-card"><h2>На связи с вашим выбором</h2><p>Сохраните понравившееся в избранное, соберите комплект в корзине или задайте вопрос помощнику. Ваш выбор сохранится в этом браузере.</p><button class="pill outline" data-open="chat-dialog">Задать вопрос ${icon('chat')}</button></div></div>`;if(type==='delivery')return `${breadcrumbs('Доставка и оплата')}<div class="info-page"><h1 class="page-heading">От выбора до встречи.</h1><p class="lead">Здесь появятся условия доставки и оплаты вашего магазина.</p><div class="info-card"><h2>Доставка</h2><p>Город, зоны доставки, сроки и стоимость пока не указаны. Эти условия нужно подтвердить перед запуском продаж.</p></div><div class="info-card"><h2>Оплата</h2><p>Онлайн-оплата пока не подключена. В демонстрационной версии вы можете собрать корзину и сохранить подборку на устройство.</p><a class="text-link" href="cart.html">Перейти к своей подборке ${icon('arrow')}</a></div><div class="info-card"><h2>Гарантия и возврат</h2><p>Условия обслуживания, реквизиты продавца и порядок возврата будут добавлены после подтверждения магазином.</p></div></div>`;return `${breadcrumbs('Помощь')}<div class="info-page"><h1 class="page-heading">Начнём с вопроса.</h1><p class="lead">Поможем сориентироваться в выборе.</p><div class="info-card"><h2>Помощник по каталогу</h2><p>Подскажет подходящую категорию и объяснит, как работают корзина и избранное. Ответы автоматические; живой менеджер пока не подключён.</p><button class="pill iridescent" data-open="chat-dialog">Открыть чат ${icon('chat')}</button></div><details><summary>Цены на сайте окончательные?</summary><p>Нет. Это примерные цены для демонстрации магазина. Ассортимент, стоимость и наличие нужно подтвердить перед началом продаж.</p></details><details><summary>Могу ли я сейчас заказать устройство?</summary><p>Пока можно собрать и скачать подборку. Отправка заказа и приём оплаты не подключены.</p></details><details><summary>Как связаться с магазином?</summary><p>Телефон, адрес и ссылка на мессенджер ещё не добавлены. После подключения здесь появятся контакты менеджера.</p></details></div>`}
function renderSearch(query=''){const matches=products.filter(p=>(p.name+' '+p.category).toLowerCase().includes(query.toLowerCase()));$('.search-results').innerHTML=matches.length?matches.map(p=>`<a class="search-result" href="${productURL(p)}"><img src="assets/${p.image}" alt=""><div>${p.name}<small>${money(p.price)}</small></div>${icon('arrow')}</a>`).join(''):'<p class="muted">Ничего не найдено. Попробуйте «iPhone» или «MacBook».</p>'}
function answer(q){if(/заказ|оплат|купить|достав|цен|налич|менедж|человек|оператор/i.test(q))return 'Это демонстрационная витрина: цены примерные, оплата и связь с менеджером ещё не подключены. Можно добавить устройства в корзину и скачать подборку. Заказ при этом не отправляется.';if(/iphone|айфон|телефон|фото/i.test(q))return 'В каталоге есть складной iPhone Duo и iPhone 18 Pro. У Duo — внутренний экран 7,6″ и чип A20 Pro; Apple указывает начало продаж 23 октября. Для обычного формата посмотрите iPhone 18 Pro.';if(/mac|ноут|работ|уч[её]б/i.test(q))return 'Для повседневной работы и учёбы посмотрите MacBook Air с M5. Если планируете монтаж или сложную графику, перед покупкой стоит уточнить процессор и объём оперативной памяти. Модель находится в разделе MacBook.';if(/музык|науш|airpods|подар/i.test(q))return 'Посмотрите AirPods Pro 3: активное шумоподавление, пространственное аудио и измерение пульса во время тренировок. Модель есть в разделе AirPods.';if(/ipad|планш|рисов/i.test(q))return 'Для чтения, заметок и творчества посмотрите iPad Air. Перед покупкой стилуса или клавиатуры обязательно проверьте совместимость с конкретной моделью.';if(/памят|гб|gb/i.test(q))return 'Объём зависит от ваших файлов: если храните много видео, фото или проектов на устройстве, выбирайте больше памяти. Доступные варианты есть в карточке товара.';if(/привет|здрав/i.test(q))return 'Здравствуйте! Выбираете телефон, ноутбук, планшет или наушники?';return 'Я пока умею отвечать на базовые вопросы о выборе iPhone, MacBook, iPad и AirPods, а также о корзине. Напишите, какое устройство вам нужно. Данные о наличии и контакты магазина ещё не подключены.'}
function sendChat(q){q=q.trim();if(!q)return;const messages=$('#messages');for(const [cls,text]of [['user',q],['',answer(q)]]){const div=document.createElement('div');div.className='message '+cls;div.textContent=text;messages.appendChild(div)}messages.scrollTop=messages.scrollHeight}
shell();$('#main').innerHTML=page==='home'?home():page==='catalog'?catalog():page==='product'?productPage():page==='cart'?cartPage():page==='favorites'?favoritePage():infoPage(page);if(page==='catalog')renderCatalog();if(page==='cart')renderCart();renderSearch();
document.addEventListener('click',e=>{const open=e.target.closest('[data-open]');if(open){openPanel(open.dataset.open)}const close=e.target.closest('[data-close]');if(close)closePanel(close.closest('dialog'));const add=e.target.closest('[data-add]');if(add)addProduct(products.find(p=>p.id===add.dataset.add),0,cardSelections[add.dataset.add]?.color||0);const fav=e.target.closest('[data-favorite]');if(fav){const id=fav.dataset.favorite;favorites=favorites.includes(id)?favorites.filter(x=>x!==id):[...favorites,id];save();if(page==='favorites')$('#main').innerHTML=favoritePage();else{fav.classList.toggle('selected',favorites.includes(id));fav.innerHTML=icon(favorites.includes(id)?'heartFill':'heart');fav.setAttribute('aria-pressed',String(favorites.includes(id)));fav.setAttribute('aria-label',`${favorites.includes(id)?'Убрать из избранного':'В избранное'}: ${products.find(p=>p.id===id).name}`)}toast(favorites.includes(id)?'Сохранено в избранном':'Удалено из избранного')}const category=e.target.closest('[data-category]');if(category){selectedCategory=category.dataset.category;const u=new URL(location.href);selectedCategory==='Все'?u.searchParams.delete('category'):u.searchParams.set('category',selectedCategory);history.replaceState(null,'',u);renderCatalog()}if(e.target.closest('#reset-filters,[data-reset]')){selectedCategory='Все';$('#filter-search').value='';$('#sort').value='default';history.replaceState(null,'','catalog.html');renderCatalog()}const mem=e.target.closest('[data-memory]'),col=e.target.closest('[data-color]');if(mem){selectedMemory=Number(mem.dataset.memory);$$('[data-memory]').forEach(b=>{const active=Number(b.dataset.memory)===selectedMemory;b.classList.toggle('active',active);b.setAttribute('aria-pressed',String(active))});$('#product-price').textContent=money(selectedProduct.price+selectedMemory*10000)}if(col){selectedColor=Number(col.dataset.color);$$('[data-color]').forEach(b=>{const active=Number(b.dataset.color)===selectedColor;b.classList.toggle('active',active);b.setAttribute('aria-pressed',String(active))});$('#color-name').textContent=selectedProduct.colors[selectedColor];galleryIndex=0;refreshGallery()}if(e.target.closest('#product-add'))addProduct(selectedProduct,selectedMemory,selectedColor);const qty=e.target.closest('[data-qty]');if(qty){const i=Number(qty.dataset.qty);cart[i].qty=Math.min(99,cart[i].qty+Number(qty.dataset.delta));cart=cart.filter(x=>x.qty>0);save();renderCart()}const remove=e.target.closest('[data-remove]');if(remove){cart.splice(Number(remove.dataset.remove),1);save();renderCart()}if(e.target.closest('#download-order')){const text='APPLE MARKET — подборка устройств\nЭто не заказ. Цены примерные.\n\n'+cart.map(x=>`${products.find(p=>p.id===x.id).name} ${x.memory}, ${x.color} — ${x.qty} шт. × ${money(x.price)}`).join('\n')+'\n\nПримерная сумма: '+money(cart.reduce((s,x)=>s+x.qty*x.price,0));const a=document.createElement('a');a.href=URL.createObjectURL(new Blob(['\uFEFF'+text],{type:'text/plain;charset=utf-8'}));a.download='APPLE MARKET-подборка.txt';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000);toast('Подборка сохранена. Заказ не отправлен.')}const question=e.target.closest('[data-question]');if(question)sendChat(question.dataset.question)});
$('.chat-form').addEventListener('submit',e=>{e.preventDefault();const input=$('.chat-form input');sendChat(input.value);input.value=''});$('.search-input').addEventListener('input',e=>renderSearch(e.target.value));if(page==='catalog'){for(const id of ['filter-search','sort'])$('#'+id).addEventListener('input',renderCatalog)}$$('dialog').forEach(d=>d.addEventListener('click',e=>{if(e.target===d){const r=d.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)closePanel(d)}}));

// Motion uses a short spring with a soft overshoot, matching the supplied menu reference.
const motionPreference=matchMedia('(prefers-reduced-motion: reduce)');
function openPanel(id){const dialog=$('#'+id);if(!dialog||dialog.open)return;dialog.showModal();const chat=id==='chat-dialog';const dockButton=$('.dock [data-open="chat-dialog"]');if(chat)dockButton.setAttribute('aria-expanded','true');if(!motionPreference.matches){const menu=id==='menu-dialog';dialog.animate([{opacity:0,transform:menu?'translate(5px,-8px) scale(.28)':'translateY(15px) scale(.93)'},{opacity:1,transform:'translate(0,0) scale(1.022)',offset:.7},{opacity:1,transform:'translate(0,0) scale(1)'}],{duration:menu?540:410,easing:'cubic-bezier(.2,.85,.25,1)'});if(menu)$$('nav a',dialog).forEach((el,i)=>el.animate([{opacity:0,transform:'translateY(-8px)'},{opacity:1,transform:'translateY(0)'}],{duration:330,delay:100+i*34,fill:'backwards',easing:'cubic-bezier(.2,1,.3,1)'}))}if(id==='search-dialog')$('.search-input').focus()}
function closePanel(dialog){if(!dialog||!dialog.open||dialog.dataset.closing)return;dialog.dataset.closing='true';const finish=()=>{dialog.close();delete dialog.dataset.closing;if(dialog.id==='chat-dialog')$('.dock [data-open="chat-dialog"]').setAttribute('aria-expanded','false')};if(motionPreference.matches){finish();return}dialog.animate([{opacity:1,transform:'scale(1)'},{opacity:0,transform:dialog.id==='menu-dialog'?'translate(6px,-6px) scale(.85)':'translateY(10px) scale(.96)'}],{duration:180,easing:'ease-in',fill:'forwards'}).finished.then(()=>{finish();dialog.getAnimations().forEach(a=>a.cancel())})}
$$('dialog').forEach(d=>d.addEventListener('cancel',e=>{e.preventDefault();closePanel(d)}));
// Decorative binary rain: fixed behind content, never captures input.
(function matrixRain(){
 const canvas=$('#matrix');if(!canvas)return;const ctx=canvas.getContext('2d');if(!ctx)return;
 let width=0,height=0,columns=[],raf=0,last=0;
 function resize(){width=innerWidth;height=innerHeight;const dpr=Math.min(devicePixelRatio||1,1.5);canvas.width=width*dpr;canvas.height=height*dpr;ctx.setTransform(dpr,0,0,dpr,0,0);const gap=width<760?29:36;columns=Array.from({length:Math.ceil(width/gap)},(_,i)=>({x:i*gap+9,offset:(i*211)%Math.max(height,1),speed:21+(i%7)*7,seed:i*17,trail:10+i%9,depth:i%3}));draw(performance.now())}
 function draw(t){
  const time=t/1000;ctx.clearRect(0,0,width,height);
  for(const col of columns){
   const size=col.depth===0?10:col.depth===1?12:14;ctx.font=`${size}px ui-monospace, SFMono-Regular, monospace`;
   const step=size+9;const y=((motionPreference.matches?0:time)*col.speed+col.offset)%(height+col.trail*step)-col.trail*step;
   const x=col.x+Math.sin(time*.32+col.seed)*3;
   for(let j=0;j<col.trail;j++){
    const yy=y+j*step;if(yy< -20||yy>height+20)continue;
    const progress=j/(col.trail-1);const wave=(Math.sin(time*.8+col.seed*.27+j*.33)+1)/2;
    const from=Math.round(55+wave*151);const to=Math.round(235-wave*122);
    const dark=document.documentElement.dataset.theme==='dark';
    const gradient=ctx.createLinearGradient(x,yy-size,x+size,yy+4);
    if(!dark){gradient.addColorStop(0,`rgba(${from},${from},${from},${(.11+progress*.33)*(col.depth===0?.7:1)})`);
    gradient.addColorStop(.5,`rgba(${Math.round((from+to)/2)},${Math.round((from+to)/2)},${Math.round((from+to)/2)},${.12+progress*.29})`);
    gradient.addColorStop(1,`rgba(${to},${to},${to},${.1+progress*.25})`);
    }else{gradient.addColorStop(0,`rgba(30,${Math.round(95+wave*65)},255,${.3+progress*.5})`);gradient.addColorStop(.5,`rgba(45,${Math.round(145+wave*55)},255,${.35+progress*.5})`);gradient.addColorStop(1,`rgba(65,125,255,${.25+progress*.45})`)}
    ctx.fillStyle=gradient;ctx.shadowColor=dark?'rgba(40,125,255,.65)':progress>.85?'rgba(255,255,255,.6)':'transparent';ctx.shadowBlur=progress>.85?5:0;
    const tick=Math.floor(time*(col.depth===0?.7:1.4)+j*.23);ctx.fillText(((col.seed+j*7+tick)%5)<2?'1':'0',x,yy);
   }
  }
  ctx.shadowBlur=0;
 }
 function frame(t){if(t-last>36){draw(t);last=t}raf=requestAnimationFrame(frame)}
 function start(){cancelAnimationFrame(raf);if(!document.hidden&&!motionPreference.matches)raf=requestAnimationFrame(frame);else draw(0)}
 addEventListener('resize',resize,{passive:true});document.addEventListener('visibilitychange',start);motionPreference.addEventListener('change',start);resize();start();
})();

// Keep navigation on the current design version when older preview pages are cached.
document.addEventListener('click',event=>{const a=event.target.closest('a[href]');if(!a||a.target==='_blank')return;const url=new URL(a.href,location.href);if(url.origin===location.origin&&url.pathname.endsWith('.html')){url.searchParams.set('v','apple-market-4');a.href=url.href}},true);

function galleryFor(p,color=0){return p.gallery?.[color]?.length?p.gallery[color]:[p.colorImages?.[color]||p.image]}
function colorHex(name){return ({'Star White':'#e8e7e2','Night Sky':'#353b45','Burgundy':'#633441','Glacier':'#acc4cf','Silver':'#cdd0d4','Black':'#292b31','Sky Blue':'#accbdd','Starlight':'#e6dbcc','Midnight':'#343e50','Blue':'#b9cedc','Purple':'#beb9d3','Space Gray':'#7a7f87','White':'#f0f0ee','Space Gray':'#888c96','Light Gold':'#e3cfb3','Dark Bronze':'#6c5b51'})[name]||'#c7cbd4'}
function productGallery(p){const images=galleryFor(p,selectedColor);return `<section class="product-gallery" aria-label="Фотографии ${p.name}"><div class="product-photo gallery-stage" tabindex="0" role="group" aria-roledescription="карусель" aria-label="Галерея ${p.name}"><img class="gallery-main" src="assets/${images[0]}" alt="${p.name} — ${p.colors[selectedColor]}, общий вид" width="900" height="900" draggable="false"><button class="gallery-arrow previous" data-gallery-step="-1" aria-label="Предыдущее фото">${icon('arrow')}</button><button class="gallery-arrow next" data-gallery-step="1" aria-label="Следующее фото">${icon('arrow')}</button><span class="gallery-counter" aria-live="polite">1 / ${images.length}</span></div><div class="gallery-thumbs" aria-label="Выбрать ракурс">${images.map((src,i)=>`<button class="gallery-thumb ${i===0?'active':''}" data-gallery-index="${i}" aria-label="Фото ${i+1} из ${images.length}" aria-pressed="${i===0}"><img src="assets/${src}" alt="" width="90" height="90" loading="lazy"></button>`).join('')}</div></section>`}
function refreshGallery(){if(!selectedProduct)return;const images=galleryFor(selectedProduct,selectedColor);galleryIndex=((galleryIndex%images.length)+images.length)%images.length;const img=$('.gallery-main');img.src='assets/'+images[galleryIndex];img.alt=selectedProduct.name+' — '+(galleryIndex===0?selectedProduct.colors[selectedColor]:'ракурс '+(galleryIndex+1));$('.gallery-counter').textContent=`${galleryIndex+1} / ${images.length}`;$('.gallery-thumbs').innerHTML=images.map((src,i)=>`<button class="gallery-thumb ${i===galleryIndex?'active':''}" data-gallery-index="${i}" aria-label="Фото ${i+1} из ${images.length}" aria-pressed="${i===galleryIndex}"><img src="assets/${src}" alt="" width="90" height="90" loading="lazy"></button>`).join('');if(!motionPreference.matches)img.animate([{opacity:.4,transform:'scale(.975)'},{opacity:1,transform:'scale(1)'}],{duration:300,easing:'cubic-bezier(.2,1,.3,1)'})}
function refreshCard(id,replace=false){const p=products.find(p=>p.id===id);if(!p)return;const state=cardSelections[id]||{color:0,index:0};const images=galleryFor(p,state.color);state.index=(state.index+images.length)%images.length;cardSelections[id]=state;const card=$(`[data-card-id="${id}"]`);if(!card)return;const track=$('.card-track',card);if(replace){track.innerHTML=cardSlides(p,state);if(!motionPreference.matches)track.animate([{opacity:.25},{opacity:1}],{duration:280})}track.style.transform=`translateX(-${state.index*100}%)`;$$('.card-slide',track).forEach((slide,i)=>slide.setAttribute('aria-hidden',String(i!==state.index)));$('.photo-dots',card).innerHTML=cardDots(p,state);$$('[data-card-color]',card).forEach(b=>{const on=Number(b.dataset.cardColor)===state.color;b.classList.toggle('active',on);b.setAttribute('aria-pressed',String(on))})}
document.addEventListener('click',event=>{const step=event.target.closest('[data-gallery-step]');if(step){galleryIndex+=Number(step.dataset.galleryStep);refreshGallery()}const thumb=event.target.closest('[data-gallery-index]');if(thumb){galleryIndex=Number(thumb.dataset.galleryIndex);refreshGallery()}const dot=event.target.closest('[data-card-index]');if(dot){const id=dot.dataset.productId;cardSelections[id]={...(cardSelections[id]||{color:0}),index:Number(dot.dataset.cardIndex)};refreshCard(id)}const color=event.target.closest('[data-card-color]');if(color){cardSelections[color.dataset.productId]={color:Number(color.dataset.cardColor),index:0};refreshCard(color.dataset.productId,true)}});
if(page==='product'&&selectedProduct){const stage=$('.gallery-stage');stage.addEventListener('keydown',event=>{if(event.key==='ArrowRight'||event.key==='ArrowLeft'){event.preventDefault();galleryIndex+=event.key==='ArrowRight'?1:-1;refreshGallery()}});let touchStart;stage.addEventListener('pointerdown',event=>{if(!event.target.closest('button'))touchStart={x:event.clientX,y:event.clientY}});stage.addEventListener('pointerup',event=>{if(touchStart&&Math.abs(event.clientX-touchStart.x)>45&&Math.abs(event.clientY-touchStart.y)<75){galleryIndex+=event.clientX<touchStart.x?1:-1;refreshGallery()}touchStart=null});stage.addEventListener('pointercancel',()=>touchStart=null)}
// Card photos are draggable independently of the product link.
let cardDrag=null;
document.addEventListener('pointerdown',e=>{const viewport=e.target.closest('[data-swipe-card]');if(!viewport||e.button!==0)return;const id=viewport.dataset.swipeCard;cardDrag={viewport,id,x:e.clientX,y:e.clientY,time:performance.now(),dx:0,index:cardSelections[id]?.index||0};viewport.setPointerCapture(e.pointerId)});
document.addEventListener('pointermove',e=>{if(!cardDrag)return;const d=cardDrag;d.dx=e.clientX-d.x;if(Math.abs(d.dx)<5)return;const track=$('.card-track',d.viewport);track.style.transition='none';track.style.transform=`translateX(calc(-${d.index*100}% + ${d.dx*.8}px))`;d.viewport.classList.add('dragging')});
function endCardDrag(e,cancel=false){if(!cardDrag)return;const d=cardDrag;cardDrag=null;const track=$('.card-track',d.viewport);track.style.transition='';d.viewport.classList.remove('dragging');const state=cardSelections[d.id]||{color:0,index:0};if(!cancel&&Math.abs(e.clientY-d.y)<100&&(Math.abs(d.dx)>40||Math.abs(d.dx)>18&&performance.now()-d.time<220)){state.index+=d.dx<0?1:-1}cardSelections[d.id]=state;refreshCard(d.id)}
document.addEventListener('pointerup',e=>endCardDrag(e));document.addEventListener('pointercancel',e=>endCardDrag(e,true));
document.addEventListener('keydown',e=>{const viewport=e.target.closest('[data-swipe-card]');if(!viewport||!['ArrowLeft','ArrowRight'].includes(e.key))return;e.preventDefault();const id=viewport.dataset.swipeCard;const state=cardSelections[id]||{color:0,index:0};state.index+=e.key==='ArrowRight'?1:-1;cardSelections[id]=state;refreshCard(id)});
if(page==='catalog'){
 const trigger=$('#sort-trigger'),menu=$('#sort-menu');let sortAnimation;
 function closeSort(focus=false){if(menu.hidden)return;trigger.setAttribute('aria-expanded','false');sortAnimation?.cancel();if(focus)trigger.focus();if(motionPreference.matches){menu.hidden=true;return}sortAnimation=menu.animate([{opacity:1,transform:'scale(1)'},{opacity:0,transform:'translateY(-5px) scale(.96)'}],{duration:150,easing:'ease-in'});sortAnimation.finished.then(()=>menu.hidden=true).catch(()=>{})}
 function openSort(){sortAnimation?.cancel();menu.hidden=false;trigger.setAttribute('aria-expanded','true');if(!motionPreference.matches)sortAnimation=menu.animate([{opacity:0,transform:'translateY(-9px) scale(.82)'},{opacity:1,transform:'translateY(0) scale(1.025)',offset:.7},{opacity:1,transform:'scale(1)'}],{duration:480,easing:'cubic-bezier(.2,.85,.25,1)'});$('[aria-checked="true"]',menu).focus({preventScroll:true})}
 trigger.addEventListener('click',()=>menu.hidden?openSort():closeSort());
 menu.addEventListener('click',e=>{const option=e.target.closest('[data-sort]');if(!option)return;$('#sort').value=option.dataset.sort;$('#sort-label').textContent=$('span',option).textContent;$$('[data-sort]',menu).forEach(b=>b.setAttribute('aria-checked',String(b===option)));renderCatalog();closeSort(true)});
 document.addEventListener('click',e=>{if(!e.target.closest('.sort-wrap'))closeSort()});
 menu.addEventListener('keydown',e=>{const options=$$('[data-sort]',menu);const i=options.indexOf(document.activeElement);if(e.key==='Escape'){e.preventDefault();e.stopPropagation();closeSort(true)}if(['ArrowDown','ArrowUp','Home','End'].includes(e.key)){e.preventDefault();const next=e.key==='Home'?0:e.key==='End'?options.length-1:(i+(e.key==='ArrowDown'?1:-1)+options.length)%options.length;options[next].focus()}if(e.key==='Tab')closeSort()});
 document.addEventListener('click',e=>{if(e.target.closest('[data-reset]')){$('#sort-label').textContent='По умолчанию';$$('[data-sort]').forEach(b=>b.setAttribute('aria-checked',String(b.dataset.sort==='default')))}})
}
// The GIF uses a static frame when paused or reduced motion is requested.
if(page==='home'){
 const gif=$('#hero-gif'),toggle=$('#video-toggle');let paused=motionPreference.matches;
 function syncGif(){gif.src=paused?'assets/iphone-duo-hero-still.jpg':'assets/iphone-duo-hero.gif';toggle.setAttribute('aria-label',paused?'Воспроизвести анимацию':'Приостановить анимацию');toggle.setAttribute('aria-pressed',String(paused));toggle.firstElementChild.textContent=paused?'▶':'Ⅱ'}
 toggle.addEventListener('click',()=>{paused=!paused;syncGif()});motionPreference.addEventListener('change',()=>{paused=motionPreference.matches;syncGif()});syncGif();
}
// A persistent theme shared by every page, applied before styles load.
const themeToggle=$('#theme-toggle');
function syncTheme(){const dark=document.documentElement.dataset.theme==='dark';themeToggle.setAttribute('aria-pressed',String(dark));themeToggle.setAttribute('aria-label',dark?'Включить светлую тему':'Включить тёмную тему');const meta=$('meta[name="theme-color"]');if(meta)meta.content=dark?'#121214':'#f4f5f7'}
themeToggle.addEventListener('click',()=>{document.documentElement.dataset.theme=document.documentElement.dataset.theme==='dark'?'light':'dark';try{localStorage.setItem('apple-market-theme',document.documentElement.dataset.theme)}catch{}syncTheme()});
addEventListener('storage',e=>{if(e.key==='apple-market-theme'){document.documentElement.dataset.theme=e.newValue==='dark'?'dark':'light';syncTheme()}});syncTheme();
// Hold the selected item, then drag to preview a destination; release to open it.
const dock=$('.dock');let navDrag=null,suppressNavClick=false;
function clearNavDrag(){if(navDrag)clearTimeout(navDrag.timer);navDrag=null;dock.classList.remove('scrubbing');$$('.dock-item').forEach(el=>el.classList.remove('scrub-target'))}
dock.addEventListener('pointerdown',e=>{const item=e.target.closest('.dock-item');if(e.button!==0||!item||!(item.classList.contains('active')||item.getAttribute('aria-expanded')==='true'))return;navDrag={id:e.pointerId,x:e.clientX,y:e.clientY,item,target:item,ready:false};navDrag.timer=setTimeout(()=>{if(!navDrag)return;navDrag.ready=true;dock.setPointerCapture(e.pointerId);dock.classList.add('scrubbing');item.classList.add('scrub-target')},180)});
dock.addEventListener('pointermove',e=>{const d=navDrag;if(!d||d.id!==e.pointerId)return;if(!d.ready){if(Math.hypot(e.clientX-d.x,e.clientY-d.y)<=10)return;clearTimeout(d.timer);d.ready=true;dock.setPointerCapture(e.pointerId);dock.classList.add('scrubbing')}const hit=document.elementFromPoint(e.clientX,e.clientY)?.closest('.dock-item');d.target=hit&&dock.contains(hit)?hit:null;$$('.dock-item').forEach(el=>el.classList.toggle('scrub-target',el===d.target))});
dock.addEventListener('pointerup',e=>{if(!navDrag||navDrag.id!==e.pointerId)return;const {ready,target,item}=navDrag;clearNavDrag();if(ready){suppressNavClick=true;setTimeout(()=>suppressNavClick=false,400);if(target&&target!==item){if(target.dataset.open)openPanel(target.dataset.open);else{const url=new URL(target.href);url.searchParams.set('v','apple-market-4');location.assign(url.href)}}}});
dock.addEventListener('pointercancel',clearNavDrag);dock.addEventListener('lostpointercapture',clearNavDrag);
dock.addEventListener('click',e=>{if(suppressNavClick){e.preventDefault();e.stopPropagation();suppressNavClick=false}},true);
dock.addEventListener('contextmenu',e=>e.preventDefault());

dock.addEventListener('dragstart',e=>e.preventDefault());
